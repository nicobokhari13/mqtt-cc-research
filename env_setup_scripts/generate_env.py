import os
import sys
import csv

def main():
    # input parammeters
        # sim or testbed = sys.argv[1]
        # if sim then 
            # num_subs = sys.argv[2]
            # num_topics = sys.argv[3]
            # threshold = sys.argv[4]
            # energy_per_execution = sys.argv[5]
            # algo_timeWindow = sys.argv[6]
            # frequency range= sys.argv[7] (50, 40, 30, 20)
            # num_publishers
        # if testbed then: 
            # num_subs = sys.argv[2]
            # num_topics = sys.argv[3]
            # threshold = sys.argv[4]
            # energy_per_execution = sys.argv[5]
            # algo_timeWindow = sys.argv[6]
            # frequency range= sys.argv[7] (50, 40, 30, 20)
            # 
        # energy per execution
        # timeWindow to run algorithm
        # in order for a device to be added to the DB, I need
            # macAddress
            # battery
            # [optional] topic capability (deviceMac, topic)
    pass

if __name__ == "__main__":
    main()